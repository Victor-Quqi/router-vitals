{
  "name": "router-vitals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Anonymous community status monitor for AnyRouter Claude Code usage.",
  "packageManager": "pnpm@11.6.0",
  "scripts": {
    "clean:build": "node -e \"require('node:fs').rmSync('.tsbuild/out',{recursive:true,force:true})\"",
    "build": "pnpm run clean:build && tsc -p tsconfig.build.json && node .tsbuild/out/_build/copy-output.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "test": "pnpm run build && node --test tests/*.test.mjs",
    "status:preview": "pnpm run build && node scripts/serve-static.mjs status-page 8788"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
